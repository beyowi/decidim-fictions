<%= decidim_form_for(@notes_form, url: fiction_fiction_notes_path(fiction, @notes_form), html: { class: "form new_fiction_note" }) do |f| %>
  <div class="row column">
    <%= f.text_area :body, rows: 10, label: t(".note") %>
  </div>
  <div class="button--double form-general-submit">
    <%= f.submit t(".submit") %>
  </div>
<% end %>
